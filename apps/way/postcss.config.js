/**
 * PostCSS config for Next.js
 * Uses framework resolver with CSS alias support (dndev.css → dndev-next.css)
 */
import { definePostCSSConfig } from '@donotdev/core/next';

export default await definePostCSSConfig();
